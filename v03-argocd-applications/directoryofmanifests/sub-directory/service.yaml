apiVersion: v1
kind: Service
metadata:
  name: nginx-sub-directory
  labels:
    app: demo-app
spec:
  type: ClusterIP
  selector:
    app: demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
